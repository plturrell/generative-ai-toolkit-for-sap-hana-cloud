Model_type: AutomaticTimeSeries. Useful for when you need to get example to create AutoML TimeSeries/AutomaticTimeSeries model.
------
Strictly use the Examples to create AutoML instance.
Do fitting:
>>> from hana_ml.algorithms.pal.auto_ml import AutomaticTimeSeries
>>> model_train = AutomaticTimeSeries(generations=2, population_size=5, offspring_size=5, max_eval_time_mins=10)
>>> model_train.enable_workload_class(workload_class_name=<WORKLOAD_CLASS>)
>>> model_train.fit(data=df, key=<key>, exog=<exog>, endog=<endog>)
>>> print({"output table names": model_train._fit_output_table_names, "Python object address": id(model_train)})