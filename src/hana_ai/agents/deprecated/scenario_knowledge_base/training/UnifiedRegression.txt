Model_type: UnifiedRegression. Useful for when you need to get example to create Unified Regression model.
------
Strictly use the Examples to create unified Regression instance.
Do fitting:
>>> from hana_ml.algorithms.pal.unified_Regression import UnifiedRegression
>>> model_train = UnifiedRegression(func=<func>)
>>> model_train.fit(data=df, key=<key>, features=<features>, label=<label>)
>>> print({"output table names": model_train._fit_output_table_names, "Python object address": id(model_train)})